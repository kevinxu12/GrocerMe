/**
 * @file Helpers for the model class
 */
export enum RequestStatus {
  REJECTED = 'REJECTED',
  ACCEPTED = 'ACCEPTED',
  AWAITING = 'AWAITING',
}

export enum ItemRequestStatus {
  REJECTED = 'REJECTED',
  ACCEPTED = 'ACCEPTED',
  AWAITING = 'AWAITING',
  SOLD = 'SOLD',
}
