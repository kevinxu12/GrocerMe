/**
 * @file Top-level entry type for the types directory
 * @author Kevin Xu
 */
import { RootState } from './RootState';
export type setMessageSnackbarType = (string) => void;
export type { RootState };
